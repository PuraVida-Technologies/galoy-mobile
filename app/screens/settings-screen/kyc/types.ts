export enum IDType {
  DriverLicense = "DRIVING_LICENSE",
  Passport = "PASSPORT",
  Other = "OTHER",
}

export enum PermissionStatus {
  UNAVAILABLE = "unavailable",
  BLOCKED = "blocked",
  DENIED = "denied",
  GRANTED = "granted",
  LIMITED = "limited",
}
